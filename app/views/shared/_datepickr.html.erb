<%= simple_form_for :search, method: "get" do |f| %>
  <%= f.input :overnight_dates, as: :string, required: false, input_html: {class: "datepicker"} %>

<!--<%= f.input :check_out, as: :string, required: false, input_html: {class: "datepicker"} %> -->
   <%= f.input :num_guests %>
  <%= f.button :submit, button_label, class: "btn btn-primary" %>
<% end %>
